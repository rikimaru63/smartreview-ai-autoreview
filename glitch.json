{
  "install": "cd backend && pip3 install -r requirements.txt && cd ../frontend && npm install",
  "start": "bash start.sh",
  "watch": {
    "include": [
      "backend/**/*.py",
      "frontend/src/**/*"
    ]
  },
  "env": {
    "PORT": "3000"
  }
}